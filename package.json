{
  "name": "command-center-bux",
  "version": "1.0.0",
  "description": "מרכז פיקוד אישי — BUX",
  "scripts": {
    "build": "rm -rf www && mkdir www && cp -r index.html css js icons manifest.json sw.js www/",
    "cap:sync": "npm run build && npx cap sync",
    "cap:ios": "npm run cap:sync && npx cap open ios",
    "cap:android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1"
  }
}
